<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/secondaryColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SynButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/synthesizeColorDark</item>
        <item name="colorButtonNormal">@color/synthesizeColor</item>
    </style>

    <style name="AnaButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/analyzeColorDark</item>
        <item name="colorButtonNormal">@color/analyzeColor</item>
    </style>

    <style name="SynthesizeHintTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/synthesizeColorLight</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="AnalyzeHintTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/analyzeColorLight</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="SynthesizeCardTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/synthesizeColorLight</item>
    </style>

    <style name="AnalyzeCardTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/analyzeColorLight</item>
    </style>

</resources>